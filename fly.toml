# fly.toml app configuration file generated for transfer-back on 2025-11-22T20:59:37Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'transfer-back'
primary_region = 'iad'

[build]

[deploy]
  release_command = 'bunx prisma migrate deploy'
  seed_command = 'tsx prisma/seed.ts'

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024
